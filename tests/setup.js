/*const config = {
	'objectType': {
		handlers: [
			(params, callback) => {
				const object = params.object;
				// TODO some logic with object that is objectType
				// call callback() to call next handler or callback(false) to stop
			},
			// ...
		],
		manipulate: {
			'otherObjectType': [
				// handlers
				(params, callback) => {
					const object = params.object;
					const otherObject = params.otherObject;
					// object manipulates on otherObject
				}
			]
		}
	}
};
*/